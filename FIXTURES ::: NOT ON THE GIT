FIXTURES :

// d'abord côté serveur :

	PosRunner.insert({"name":"gauche"})
	PosRunner.insert({"name":"droite"})

	superGlobals.insert({"isItVictoryYet":false})

// faut virer les bonhommes

	Bonhomme.remove({})


// ensuite côté client :

	Session.set("localName", "gauche");
// sur l'ordi de gauche

	Session.set("localName", "droite");
// sur l'ordi de droite

// y'a sans doutes d'autres collections à initialiser, chépu

	FukinScore.insert({"name":"gauche"})
	FukinScore.insert({"name":"droite"})

winners
viewSwitcher 


et sans soute des superGlobales